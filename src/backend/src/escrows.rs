use crate::memory::{get_escrows, with_escrows};
use crate::types::Escrow;
use std::collections::HashMap;

// This module will contain escrow business logic functions
// For now, just re-export the safe memory functions
pub use crate::memory::{get_escrows, with_escrows};
